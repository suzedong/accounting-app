# 界面设计

## 1. 设计原则

- **保留现有 UI 风格**：现有 HTML/CSS 已有完整的视觉设计，重构期间不改变视觉风格
- **桌面优化**：固定窗口尺寸，支持窗口拖拽、最小化/最大化
- **离线状态展示**：同步状态可视化
- **减少层级**：桌面应用可承载更多功能在同一页面，减少页面跳转

## 2. 页面结构

### 2.1 页面清单（保持现有 5 页不变）

| 页面 | 说明 | 改动 |
|---|---|---|
| 首页（index.html） | AI 对话 + 统计概览 | 最小改动，仅改数据获取方式 |
| 记录（records.html） | 记录增删改查 + 分页 | 最小改动 |
| 预算（budget.html） | 预算管理 | 最小改动 |
| 统计（stats.html） | 多维度图表分析 | 最小改动 |
| 差旅补助（trip_allowance.html） | 差旅管理 | 最小改动 |

### 2.2 新增页面

| 页面 | 说明 |
|---|---|
| 设置（settings.html） | API Key、NocoBase URL、同步设置、数据导入导出 |

## 3. 桌面端增强

### 3.1 窗口行为

- 固定最小尺寸：900×600
- 记住窗口位置和尺寸（配置文件存储）
- 关闭时最小化到托盘（可选）

### 3.2 系统通知

记账成功/失败时发送系统通知：

```rust
// Rust 后端调用
#[tauri::command]
fn show_notification(title: String, body: String) {
    // 使用 tauri-plugin-notification
}
```

### 3.3 全局快捷键

- `Cmd/Ctrl + Shift + A`：快速记账（呼出窗口并聚焦输入框）

## 4. 新增 UI 元素

### 4.1 同步状态指示器

在导航栏或页脚显示同步状态：

```
┌──────────────────────────────────────────────┐
│ 首页 记录 预算 统计 差旅    [🔄 同步] ● 已同步 │
└──────────────────────────────────────────────┘

状态：
● 已同步     - 绿色圆点
↻ 同步中     - 旋转图标
○ 未同步     - 灰色圆点（无连接）
⚠ 有冲突     - 黄色警告
```

### 4.2 设置页面

```
┌──────────────────────────────────────────────┐
│  设置                                         │
├──────────────────────────────────────────────┤
│                                              │
│  AI 设置                                      │
│  ┌─────────────────────────────────────────┐ │
│  │ API Key:   [**********************]     │ │
│  │ API URL:   [https://coding.dashscope...] │ │
│  │ 模型:      [qwen3.6-plus          ]     │ │
│  └─────────────────────────────────────────┘ │
│                                              │
│  NocoBase 同步                                │
│  ┌─────────────────────────────────────────┐ │
│  │ 同步地址: [http://121.17.49.100:13000] │ │
│  │ API Token: [**********************]     │ │
│  │ [立即同步]  [导入数据]  [导出CSV]        │ │
│  └─────────────────────────────────────────┘ │
│                                              │
│  预算设置                                     │
│  ┌─────────────────────────────────────────┐ │
│  │ 月度预算: [3500] 元                      │ │
│  └─────────────────────────────────────────┘ │
│                                              │
│  关于                                        │
│  ┌─────────────────────────────────────────┐ │
│  │ 版本 1.0.0 | RapidOCR | SQLite          │ │
│  └─────────────────────────────────────────┘ │
│                                              │
└──────────────────────────────────────────────┘
```

## 5. 现有页面改动说明

### 5.1 首页（AI 对话）

**布局不变**，改动仅在数据获取层：

- 记录列表从 `fetch('/api/records')` 改为 `invoke('get_records')`
- 对话历史从 localStorage 改为 SQLite
- 图片 OCR 从 `fetch('/api/ai/ocr')` 改为 `invoke('ocr_recognize')`
- AI 调用从 `fetch('/api/ai/dispatch')` 改为前端直连百炼 API

**新增元素**：同步状态指示器

### 5.2 记录管理页

**布局不变**，改动在数据源：

- 所有 CRUD 操作从 NocoBase API 改为 SQLite
- 筛选和分页由 SQL 查询完成（不再前端计算）
- 分页器保留，SQL OFFSET/LIMIT 实现

### 5.3 预算页

**布局不变**：

- 预算数据从 SQLite 读取
- 统计计算由 SQL GROUP BY 完成

### 5.4 统计页

**布局不变**：

- 数据获取从 `pageSize=10000` 改为 SQL 聚合查询
- Chart.js 渲染不变

### 5.5 差旅补助页

**布局不变**：

- 数据从 SQLite 读取
- 补助计算逻辑不变

## 6. 视觉风格

保持现有配色方案：

```css
:root {
    --primary: #667eea;
    --primary-dark: #5568d3;
    --gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --success: #52C41A;
    --warning: #FFC107;
    --danger: #FF6B6B;
    --danger-dark: #FF4D4F;
    --bg: #f5f7fa;
    --card-bg: white;
    --text: #333;
    --text-secondary: #666;
    --text-muted: #999;
}
```

桌面端新增：

```css
/* 同步状态 */
--sync-success: #68d391;
--sync-warning: #f6ad55;
--sync-error: #fc8181;
```

## 7. 响应式策略

桌面应用不需要响应式适配，但保留现有 CSS 以兼容：
- 窗口缩放时的内容自适应
- 多显示器场景
